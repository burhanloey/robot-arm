import * as THREE from 'three';

export const brown = new THREE.MeshBasicMaterial( { color: 0x8b4513,
                                                    side: THREE.DoubleSide,
                                                    wireframe: true } );
